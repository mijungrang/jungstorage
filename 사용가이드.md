# 인천시보 크롤링 사용 가이드 (초보자용)

## 🎯 빠른 시작 (5분 안에 시작하기!)

### 단계 1: Google Sheets 열기

1. 웹브라우저에서 https://sheets.google.com 접속
2. 우측 하단의 `+` 버튼 클릭 (새 스프레드시트 만들기)
3. 스프레드시트 이름 변경: "인천시보 데이터"

### 단계 2: Apps Script 편집기 열기

1. 상단 메뉴에서 `확장 프로그램` 클릭
2. `Apps Script` 선택
3. 새 탭이 열립니다

### 단계 3: 코드 복사하기

1. GitHub 또는 파일에서 `인천시보크롤링.gs` 파일 열기
2. 전체 코드 복사 (Ctrl+A, Ctrl+C)
3. Apps Script 편집기로 돌아가기
4. 기존 코드 삭제하고 붙여넣기 (Ctrl+A, Ctrl+V)
5. 저장 버튼 클릭 (💾 아이콘)

### 단계 4: 실행하기

1. 함수 선택 드롭다운에서 `crawlIncheonGazette` 선택
2. ▶️ 실행 버튼 클릭
3. 처음 실행 시:
   ```
   [권한 필요] → [권한 검토] → [Google 계정 선택]
   → [고급] → [프로젝트 이름(안전하지 않음)으로 이동]
   → [허용]
   ```

### 단계 5: 결과 확인

1. Google Sheets 탭으로 돌아가기
2. 하단에 "인천시보" 시트 생성 확인
3. 데이터 수집 완료! 🎉

---

## 📖 상세 설명

### 크롤링되는 데이터

각 인천시보 항목에 대해 다음 정보가 수집됩니다:

#### 기본 정보
- **번호**: 게시글 번호
- **제목**: 시보 제목
- **일자**: 발행 일자
- **조회수**: 조회 수
- **상세URL**: 상세 페이지 링크

#### 상세 정보 (상세 페이지에서 수집)
- **시보번호**: 시보 제호 (예: "제1234호")
- **조례**: 조례 내용 (최대 5개 항목)
- **공고**: 공고 내용 (최대 5개 항목)
- **기타**: 기타 내용 (최대 5개 항목)
- **PDF링크**: PDF 파일 다운로드 링크

### 페이지 범위 변경하기

기본적으로 1~5페이지를 크롤링합니다. 다른 범위를 원하시면:

1. Apps Script 편집기에서 함수 수정:
   ```javascript
   // 예시 1: 1~3페이지만 크롤링
   function crawlFirst3Pages() {
     crawlIncheonGazette(1, 3);
   }

   // 예시 2: 10~15페이지 크롤링
   function crawl10to15() {
     crawlIncheonGazette(10, 15);
   }
   ```

2. 새로운 함수 저장
3. 함수 선택 드롭다운에서 새 함수 선택
4. 실행

### 커스텀 메뉴 사용하기

코드를 실행하면 스프레드시트 상단에 "인천시보 크롤링" 메뉴가 추가됩니다.

1. Google Sheets 새로고침 (F5)
2. 상단 메뉴 확인: `파일` `수정` `보기` ... `인천시보 크롤링`
3. `인천시보 크롤링` > `1~5페이지 크롤링` 클릭
4. 실행!

---

## 🛠️ 고급 사용법

### 1. 자동 실행 설정 (매일 자동 크롤링)

1. Apps Script 편집기에서 왼쪽 메뉴의 ⏰ (트리거) 클릭
2. 우측 하단 `+ 트리거 추가` 클릭
3. 설정:
   - 실행할 함수: `crawlIncheonGazette`
   - 이벤트 소스: `시간 기반`
   - 시간 기반 트리거 유형: `일 타이머`
   - 시간 선택: 원하는 시간 (예: 오전 9시-10시)
4. 저장

이제 매일 자동으로 크롤링됩니다!

### 2. 특정 날짜 범위만 필터링

크롤링 후 Google Sheets에서:

1. 데이터 범위 선택
2. `데이터` > `필터 만들기`
3. 일자 컬럼의 필터 아이콘 클릭
4. 원하는 날짜 범위 선택

### 3. 데이터 내보내기

#### Excel 파일로 저장
1. `파일` > `다운로드` > `Microsoft Excel(.xlsx)`

#### CSV 파일로 저장
1. `파일` > `다운로드` > `쉼표로 구분된 값(.csv)`

#### PDF로 저장
1. `파일` > `다운로드` > `PDF 문서(.pdf)`

---

## ⚙️ 코드 커스터마이징

### 수집하는 항목 개수 변경

조례/공고/기타 항목을 5개가 아닌 다른 개수로 수집하려면:

`parseDetailPage` 함수에서:

```javascript
// 기존 (5개)
if (detail[currentSection].length < 5) {
  detail[currentSection].push(line);
}

// 10개로 변경
if (detail[currentSection].length < 10) {
  detail[currentSection].push(line);
}
```

그리고 `addItemToSheet` 함수도 수정:

```javascript
// 조례 10줄
for (let i = 0; i < 10; i++) {
  row.push(ordinances[i] || '');
}
```

헤더도 수정:

```javascript
const headers = [
  '번호', '제목', '일자', '조회수', '상세URL', '시보번호',
  '조례1', '조례2', '조례3', '조례4', '조례5',
  '조례6', '조례7', '조례8', '조례9', '조례10',
  // ... 공고, 기타도 마찬가지
];
```

### 크롤링 속도 조정

서버 부하를 줄이기 위해 딜레이가 설정되어 있습니다:

```javascript
// 항목 간 딜레이 (기본 1초)
Utilities.sleep(1000);  // 밀리초 단위

// 페이지 간 딜레이 (기본 2초)
Utilities.sleep(2000);
```

더 빠르게 하려면 숫자를 줄이세요 (예: 500ms = 0.5초)
더 느리게 하려면 숫자를 늘리세요 (예: 5000ms = 5초)

⚠️ 주의: 너무 빠르면 서버에서 차단할 수 있습니다!

---

## 🐛 문제 해결

### "권한이 없습니다" 오류

**원인**: Google이 처음 실행 시 권한을 요청합니다.

**해결**:
1. "권한 검토" 클릭
2. Google 계정 선택
3. "이 앱은 확인되지 않았습니다" 화면이 나오면:
   - "고급" 클릭
   - "프로젝트 이름(안전하지 않음)으로 이동" 클릭
   - "허용" 클릭

### "시간 초과" 오류

**원인**: 한 번에 너무 많은 데이터를 크롤링하려고 합니다.

**해결**:
1. 페이지 수를 줄이세요:
   ```javascript
   crawlIncheonGazette(1, 2);  // 2페이지만
   ```
2. 여러 번 나누어 실행:
   ```javascript
   // 첫 번째 실행
   crawlIncheonGazette(1, 5);

   // 두 번째 실행 (나중에)
   crawlIncheonGazette(6, 10);
   ```

### "데이터가 수집되지 않습니다"

**원인**: 웹사이트 구조가 변경되었을 수 있습니다.

**해결**:
1. Apps Script 실행 로그 확인:
   - 상단 메뉴 `보기` > `로그`
   - 오류 메시지 확인
2. 최신 버전의 코드로 업데이트
3. 여전히 문제가 있으면 이슈 등록

### PDF 파일을 직접 다운로드하고 싶어요

Google Apps Script는 PDF를 직접 다운로드할 수 없지만, PDF 링크를 제공합니다.

**방법 1: 수동 다운로드**
1. Google Sheets에서 PDF링크 컬럼의 링크 복사
2. 브라우저에서 열기
3. 다운로드

**방법 2: Python 스크립트 사용**
- `crawl_incheon_gazette.py` 스크립트는 PDF를 자동 다운로드합니다
- README.md의 Python 섹션 참조

---

## 💡 팁과 트릭

### 1. 데이터 정렬하기

일자별로 정렬:
1. 일자 컬럼 헤더 선택
2. `데이터` > `범위 정렬` > `열 C로 정렬(Z → A)` (최신순)

### 2. 중복 제거

같은 시보가 여러 번 크롤링된 경우:
1. 전체 데이터 선택
2. `데이터` > `중복 항목 제거`
3. "번호" 또는 "제목" 열 기준으로 선택

### 3. 조건부 서식으로 강조하기

특정 키워드가 있는 행 강조:
1. 데이터 범위 선택
2. `서식` > `조건부 서식`
3. 조건 추가:
   - `텍스트 포함`
   - 값: "조례" (또는 원하는 키워드)
4. 서식 스타일 선택

### 4. 피벗 테이블로 분석

날짜별 시보 개수 분석:
1. `삽입` > `피벗 테이블`
2. 행: 일자
3. 값: 개수 (번호)

---

## 📚 참고 자료

- [Google Apps Script 공식 문서](https://developers.google.com/apps-script)
- [Google Sheets 함수 참조](https://support.google.com/docs/table/25273)
- [인천시 공식 웹사이트](https://www.incheon.go.kr)

---

## ❓ FAQ

**Q: 무료로 사용할 수 있나요?**
A: 네! Google Apps Script는 무료입니다.

**Q: 크롤링 속도가 느려요.**
A: 서버 부하 방지를 위해 의도적으로 딜레이가 있습니다. "코드 커스터마이징" 섹션을 참조하세요.

**Q: PDF를 자동으로 Google Drive에 저장할 수 있나요?**
A: 현재 버전에서는 PDF 링크만 제공됩니다. 향후 업데이트 예정입니다.

**Q: 다른 지역의 시보도 크롤링할 수 있나요?**
A: 네, 코드를 수정하면 가능합니다. URL과 파싱 로직을 해당 웹사이트에 맞게 수정하세요.

**Q: 크롤링이 법적으로 문제없나요?**
A: 공공 데이터를 개인 학습/연구 목적으로 사용하는 것은 일반적으로 문제없지만, 상업적 사용 시 주의가 필요합니다.

---

**도움이 더 필요하신가요?**
- 이슈 등록: [GitHub Issues]
- 이메일 문의: (이메일 주소)

즐거운 데이터 수집 되세요! 🎉
